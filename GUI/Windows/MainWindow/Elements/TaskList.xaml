<UserControl
    x:Class="GUI.Windows.MainWindow.Elements.TaskList"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="1" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <ItemsControl Grid.Column="0" Items="{Binding Tasks}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border
      BorderBrush="Black"
      BorderThickness="2"
      CornerRadius="3"
      Margin="-1">
            <TextBlock
                      Text="w"/>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
    <GridSplitter
            Grid.Column="1"
            Width="1"
            HorizontalAlignment="Stretch" />
    <ItemsControl Grid.Column="2" Items="{Binding Tasks}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <TextBlock
                    Text="g11111111"/>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
  </Grid>
</UserControl>
